queue:
  worker:
    promotion:
      queue-id: default
      batch-size: 50
      max-active-count: 100
      active-ttl-seconds: 180

    expiration:
      enabled: true
      fixed-delay-ms: 1000
      batch-size: 50

  kafka:
    lifecycle-topic: queue.lifecycle.v1