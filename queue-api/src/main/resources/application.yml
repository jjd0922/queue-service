queue:
  worker:
    promotion:
      enabled: true
      fixed-delay-ms: 1000
      queue-id: default
      batch-size: 50
      max-active-count: 100
      active-ttl-seconds: 180

    expiration:
      enabled: true
      fixed-delay-ms: 1000
      batch-size: 50